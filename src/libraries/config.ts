const config = {
  BASE_URL: "http://localhost:3000/",
  CMS: {
    CONTENT_LIFECYCLE: {
      STATUS: {
        DRAFT: "DRAFT",
        PUBLISHED: "PUBLISHED",
      },
      VISIBILITY: {
        HIDDEN: "HIDDEN",
        VISIBLE: "VISIBLE",
      },
    },
  },
};

export default config;
